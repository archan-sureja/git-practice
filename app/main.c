#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main () {

    // comment for i
    int i = 12;

    // comment for conditinal
    if (i == 12){
        printf("i is twelve");
    }

    // comment for return statement.

    // amended comments.
    return 0;
}